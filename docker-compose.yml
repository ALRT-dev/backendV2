services:
  app:
    container_name: app
    build: .
    env_file:
      - .env.prod
    ports:
      - "3000:9000"
    command: sh -c "npx prisma migrate deploy && yarn start"
    restart: unless-stopped